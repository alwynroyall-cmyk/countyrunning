pandas>=1.5.0
openpyxl>=3.0.10
xlrd>=2.0.1
Pillow>=10.0.0
python-docx>=1.1.0
docx2pdf>=0.1.8
playwright>=1.50.0

# PDF export via docx2pdf requires Microsoft Word on Windows.
# On macOS/Linux, PDF output is skipped unless a compatible converter is available.
