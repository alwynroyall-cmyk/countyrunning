pandas>=1.5.0,<3.0
openpyxl>=3.0.10,<4.0
xlrd>=2.0.1,<3.0
Pillow>=10.0.0,<11.0
python-docx>=1.1.0,<2.0
docx2pdf>=0.1.8
playwright>=1.50.0,<2.0
PySide6>=6.8.0,<7.0

# Notes:
# - `docx2pdf` requires Microsoft Word (Windows/macOS) to produce PDFs; when
#   Word is not available the application will skip PDF generation.
# - `PySide6` is used to support an optional Qt-based frontend for the GUI.
# - Version upper-bounds are conservative to improve reproducibility for this
#   desktop application; adjust when upgrading and testing against newer
#   dependency releases.
